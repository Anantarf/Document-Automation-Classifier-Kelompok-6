version: "3.8"
services:
  backend:
    build: .
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - ./:/src
    ports:
      - "8000:8000"
    depends_on: []

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    environment:
      - VITE_API_BASE=http://backend:8000
    depends_on:
      - backend
